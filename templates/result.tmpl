<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Cluster-API Workload Cluster Created</title>
</head>
<body>
    <h2>CAPI Workload Cluster Helm and ArgoCD app charts pushed!<h2>
    <h4>{{ .msg }}</h4>

    <a href="{{ .giturl }}">Git Commit</a><br>
    <a href="{{ .argoCDurl }}">ArgoCD Application</a>
</body>
